<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rl"
    android:layout_width="@dimen/leftdrawer_width"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <RelativeLayout
        android:id="@+id/toprl"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_marginTop="50dp">

        <com.zdx.youpai.view.CircleImageView
            android:id="@+id/login_tv"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_centerInParent="true"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_default" />

        <LinearLayout
            android:id="@+id/animll_id"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:orientation="horizontal">

            <com.zdx.youpai.view.CircleImageView
                android:id="@+id/iv_main_left_head"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:layout_gravity="center_horizontal"
                android:src="@drawable/img_1"/>

            <TextView
                android:id="@+id/user_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_toLeftOf="@id/iv_main_left_head"
                android:layout_marginLeft="10dp"
                android:textColor="#FFF"
                android:textSize="32sp"
                android:text="hahah"/>
        </LinearLayout>
    </RelativeLayout>
    <!--允许fast scroll thumb可以拖动来快速滚动列表
定义在scrolling时哪个滚动条应该显示出来，或者不显示。-->
    <ListView
        android:id="@+id/id_leftmenu_listView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:cacheColorHint="#00000000"
        android:choiceMode="singleChoice"

        android:dividerHeight="0.0dp"
        android:drawSelectorOnTop="false"
        android:fastScrollEnabled="false"
        android:scrollbars="none">

    </ListView>
</LinearLayout>

